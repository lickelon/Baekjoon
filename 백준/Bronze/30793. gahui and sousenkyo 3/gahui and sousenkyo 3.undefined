#include <bits/stdc++.h>

#define INF 0x3FFFFFFF

using namespace std;

using ll = long long;
using ld = long double;
using pii = pair<int,int>;
using pll = pair<ll, ll>;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    float p, r;
    cin >> p >> r;
    float v = p / r;
    if(v < 0.2f) print("weak");
    else if(v < 0.4f) print("normal");
    else if(v < 0.6f) print("strong");
    else print("very strong");

    return 0;
}