#include <bits/stdc++.h>

#define INF 0x7FFFFFFF

using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    cout.precision(15);

    double L;
    cin >> L;
    cout << L * L * sqrt(3) / 4;
}