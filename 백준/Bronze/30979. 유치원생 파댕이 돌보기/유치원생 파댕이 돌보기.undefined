#include <bits/stdc++.h>

#define INF 0x3FFFFFFF

using namespace std;

using ll = long long;
using ld = long double;
using pii = pair<int,int>;
using pll = pair<ll, ll>;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    int t;
    cin >> t;
    int n;
    cin >> n;
    vector<int> arr(n);
    for(auto &e : arr) cin >> e;
    int x = ranges::fold_left(arr, 0, plus<int>());
    print("Padaeng_i {}", x >= t ? "Happy" : "Cry");
    return 0;
}